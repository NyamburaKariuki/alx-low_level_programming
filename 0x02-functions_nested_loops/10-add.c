#include "main.h"

/**
   *add - returning the sum of 2 numbers
    * @a: parameter 1 being added
    *@b: second parameter
     * Return: Always 0 (Success)
      */

int add(int a, int b)
{
	return (a + b);
}

